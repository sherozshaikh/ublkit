# Uncomment this workflow when ready to enable automatic PyPI publishing
# Currently commented out for manual publishing control

# name: Publish to PyPI
# 
# on:
#   release:
#     types: [published]
# 
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     
#     steps:
#     - uses: actions/checkout@v3
#     
#     - name: Set up Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.11'
#     
#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install build twine
#     
#     - name: Build package
#       run: python -m build
#     
#     - name: Publish to Test PyPI
#       env:
#         TWINE_USERNAME: __token__
#         TWINE_PASSWORD: ${{ secrets.TEST_PYPI_API_TOKEN }}
#       run: |
#         python -m twine upload --repository testpypi dist/*
#     
#     - name: Publish to PyPI
#       env:
#         TWINE_USERNAME: __token__
#         TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
#       run: |
#         python -m twine upload dist/*

# Manual Publishing Instructions:
# ================================
# 1. Update version in pyproject.toml
# 2. Build the package:
#    python -m build
# 3. Upload to Test PyPI:
#    python -m twine upload --repository testpypi dist/*
# 4. Test installation:
#    pip install --index-url https://test.pypi.org/simple/ ublkit
# 5. Upload to PyPI:
#    python -m twine upload dist/*
